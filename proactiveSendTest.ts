/// hidden this page
// 用於命令列直接執行，模擬主動發送訊息
// import { sendProactiveMessages } from "./teamsBot";

(async () => {
  console.log("開始主動發送 Hi 給 blob 內所有 Teams 使用者...");
  // await sendProactiveMessages();
  console.log("主動發送完成。");
})();